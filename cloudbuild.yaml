steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    args:
      - gcloud
      - beta
      - functions
      - deploy
      - text-to-speech
      - --gen2
      - --region=us-central1
      - --source=.
      - --runtime=nodejs16
      - --trigger-http
      - --allow-unauthenticated
      - --entry-point=helloHttp
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    args:
      - npm
      - ci
    waitFor: ['-']
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    args:
      - npm
      - run
      - build
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    args:
      - firebase
      - deploy
      - --only=hosting
